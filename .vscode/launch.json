{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Rails Server",
      "type": "rdbg",
      "request": "launch",
      "command": "bin/rails",
      "script": "server",
      "cwd": "${workspaceFolder}"
    }
  ]
}